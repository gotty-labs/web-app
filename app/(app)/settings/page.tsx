/**
 * Settings — email verification, console exclusions, and password (Phase 5, Slice G).
 * Thin mount of the profile feature's `SettingsView`.
 */
import { SettingsView } from '@/features/profile'

export default function SettingsPage() {
  return <SettingsView />
}
