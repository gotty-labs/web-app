/**
 * Dictionary loading (Next docs' "Localization" pattern). Translations live in
 * `dictionaries/<locale>.json` — the SINGLE source of copy, used everywhere
 * (errors + UI + game enum labels). The JSON is dynamically imported so only the
 * ACTIVE locale's file is code-split into the bundle (works on server AND client).
 *
 * The shape is validated with Zod on load (fail-loud): a missing/renamed key in
 * any locale file throws here instead of rendering `undefined`. The `Dictionary`
 * TYPE is inferred from that schema — one source of truth. Game enum labels MUST
 * be localized here (never humanized in code), so a Spanish user reads "Rumoreado",
 * not "Rumored".
 */
import { z } from 'zod'

import {
  gameAgeRatingSchema,
  gameCategorySchema,
  gameGenreSchema,
  gameLanguageCategorySchema,
  gameLanguageTypeSchema,
  gameLibraryProgressStateSchema,
  gameReleaseRegionSchema,
  gameReleaseStatusSchema,
  gameStatusSchema,
  gameThemeSchema,
  userGameLibraryStatusSchema,
} from '@/lib/domain/enums'

import { defaultLocale, type Locale } from './locales'

const errorByCodeSchema = z.object({
  invalidCredentials: z.string(),
  resetLinkExpired: z.string(),
  userNotFound: z.string(),
  invalidOAuthCredentials: z.string(),
  guestNotAllowed: z.string(),
  registerUnavailable: z.string(),
  oauthAlreadyRegistered: z.string(),
  invalidVerificationCode: z.string(),
  gameNotFound: z.string(),
  listNameExists: z.string(),
  webNotAllowed: z.string(),
})

/** A localized label for EVERY value of an enum — fail-loud if a locale omits one. */
const enumLabels = (values: readonly string[]) =>
  z.object(Object.fromEntries(values.map((value) => [value, z.string()])))

/** A landing block with a heading + supporting copy (feature cards, how-it-works steps). */
const landingFeatureSchema = z.object({
  title: z.string(),
  description: z.string(),
})

export const dictionarySchema = z.object({
  errors: z.object({
    byCode: errorByCodeSchema,
    generic: z.object({
      rateLimit: z.string(),
      server: z.string(),
      network: z.string(),
      validation: z.string(),
      sessionExpired: z.string(),
      unknown: z.string(),
    }),
  }),
  games: z.object({
    status: enumLabels(gameStatusSchema.options),
    category: enumLabels(gameCategorySchema.options),
    genre: enumLabels(gameGenreSchema.options),
    theme: enumLabels(gameThemeSchema.options),
    progressState: enumLabels(gameLibraryProgressStateSchema.options),
    libraryStatus: enumLabels(userGameLibraryStatusSchema.options),
    region: enumLabels(gameReleaseRegionSchema.options),
    releaseStatus: enumLabels(gameReleaseStatusSchema.options),
    ageRating: enumLabels(gameAgeRatingSchema.options),
    language: enumLabels(gameLanguageTypeSchema.options),
    languageCategory: enumLabels(gameLanguageCategorySchema.options),
  }),
  app: z.object({
    brand: z.object({
      name: z.string(),
      tagline: z.string(),
    }),
    advertising: z.object({
      label: z.string(),
    }),
    appDownloadPrompt: z.object({
      title: z.string(),
      description: z.string(),
      appStoreLabel: z.string(),
      googlePlayLabel: z.string(),
    }),
    adBlockPrompt: z.object({
      title: z.string(),
      description: z.string(),
      reload: z.string(),
      continue: z.string(),
    }),
    actions: z.object({
      enterApp: z.string(),
      signIn: z.string(),
      signUp: z.string(),
      continue: z.string(),
      cancel: z.string(),
      back: z.string(),
      retry: z.string(),
      close: z.string(),
    }),
    landing: z.object({
      heroBadge: z.string(),
      heroTitleLead: z.string(),
      heroTitleAccent: z.string(),
      heroSubtitle: z.string(),
      primaryCta: z.string(),
      secondaryCta: z.string(),
      nav: z.object({
        features: z.string(),
        how: z.string(),
      }),
      mock: z.object({
        trending: z.string(),
        library: z.string(),
        search: z.string(),
      }),
      features: z.object({
        title: z.string(),
        subtitle: z.string(),
        discover: landingFeatureSchema,
        search: landingFeatureSchema,
        library: landingFeatureSchema,
        progress: landingFeatureSchema,
      }),
      how: z.object({
        title: z.string(),
        subtitle: z.string(),
        steps: z.array(landingFeatureSchema).length(3),
      }),
      cta: z.object({
        title: z.string(),
        subtitle: z.string(),
        button: z.string(),
      }),
      footer: z.object({
        rights: z.string(),
      }),
    }),
    nav: z.object({
      games: z.string(),
      options: z.string(),
      feed: z.string(),
      library: z.string(),
      search: z.string(),
      consoleVisibility: z.string(),
      feedback: z.string(),
      toggleSidebar: z.string(),
    }),
    consoleVisibility: z.object({
      title: z.string(),
      description: z.string(),
      showAll: z.string(),
      hideAll: z.string(),
      save: z.string(),
      savedToast: z.string(),
    }),
    feedback: z.object({
      title: z.string(),
      description: z.string(),
      typeLabel: z.string(),
      typeIdea: z.string(),
      typeImprovement: z.string(),
      typeProblem: z.string(),
      typeOther: z.string(),
      messageLabel: z.string(),
      messagePlaceholder: z.string(),
      replyLabel: z.string(),
      emailPlaceholder: z.string(),
      emailInvalid: z.string(),
      submit: z.string(),
      sentToast: z.string(),
    }),
    profile: z.object({
      account: z.string(),
      settings: z.string(),
      notifications: z.string(),
      consoleExclusions: z.string(),
      logout: z.string(),
      version: z.string(),
    }),
    feed: z.object({
      loadMore: z.string(),
      empty: z.string(),
      error: z.string(),
      consoleVisibilityNotice: z.object({
        title: z.string(),
        description: z.string(),
        action: z.string(),
      }),
    }),
    search: z.object({
      placeholder: z.string(),
      submit: z.string(),
      consoles: z.string(),
      content: z.string(),
      genreTheme: z.string(),
      all: z.string(),
      genres: z.string(),
      themes: z.string(),
      prompt: z.string(),
      noResults: z.string(),
      reset: z.string(),
      clear: z.string(),
      closePanel: z.string(),
      apply: z.string(),
      applyError: z.string(),
      consolesPanelTitle: z.string(),
      consolesPanelDescription: z.string(),
      contentPanelTitle: z.string(),
      contentPanelDescription: z.string(),
      noConsolesSelected: z.string(),
      consoleSelected: z.string(),
      consolesSelected: z.string(),
      noContentSelected: z.string(),
      contentSelected: z.string(),
      clearConsoles: z.string(),
      clearContent: z.string(),
      noQueryResults: z.string(),
      noFilterResults: z.string(),
    }),
    detail: z.object({
      save: z.string(),
      saved: z.string(),
      whitelist: z.string(),
      whitelistShort: z.string(),
      remove: z.string(),
      lists: z.string(),
      progressLabel: z.string(),
      comingSoon: z.string(),
      updateProgress: z.string(),
      signInToSave: z.string(),
      moreActions: z.string(),
      savedToast: z.string(),
      whitelistToast: z.string(),
      removedToast: z.string(),
      listDialogTitle: z.string(),
      listLibraryDisclaimer: z.string(),
      listsUpdatedToast: z.string(),
      listEmptyTitle: z.string(),
      listEmptyDescription: z.string(),
      listIncluded: z.string(),
      about: z.string(),
      storyline: z.string(),
      readStoryline: z.string(),
      genres: z.string(),
      themes: z.string(),
      platforms: z.string(),
      languages: z.string(),
      engines: z.string(),
      ageRating: z.string(),
      releases: z.string(),
      media: z.string(),
      viewGallery: z.string(),
      mediaArtworks: z.string(),
      mediaScreenshots: z.string(),
      mediaVideos: z.string(),
      playVideo: z.string(),
      fullscreen: z.string(),
      previous: z.string(),
      next: z.string(),
      seeAll: z.string(),
      showMore: z.string(),
      showLess: z.string(),
      addToCalendar: z.string(),
      appleCalendar: z.string(),
      googleCalendar: z.string(),
      dlcs: z.string(),
      expansions: z.string(),
      timeToBeatQuick: z.string(),
      timeToBeatAverage: z.string(),
      timeToBeatTotal: z.string(),
      releaseDate: z.string(),
      developer: z.string(),
      publisher: z.string(),
      timeToBeat: z.string(),
      progress: z.object({
        title: z.string(),
        description: z.string(),
        durationLabel: z.string(),
        durationPlaceholder: z.string(),
        save: z.string(),
        updatedToast: z.string(),
      }),
    }),
    library: z.object({
      title: z.string(),
      savedTab: z.string(),
      whitelistTab: z.string(),
      listsLabel: z.string(),
      allLists: z.string(),
      newList: z.string(),
      newListDescription: z.string(),
      nameLabel: z.string(),
      namePlaceholder: z.string(),
      nameRequired: z.string(),
      nameTooLong: z.string(),
      nameExists: z.string(),
      colorLabel: z.string(),
      customColorLabel: z.string(),
      colorHexLabel: z.string(),
      colorInvalid: z.string(),
      iconLabel: z.string(),
      gameSearchLabel: z.string(),
      gameSearchPlaceholder: z.string(),
      gameSearchNoResults: z.string(),
      gameSearchHint: z.string(),
      selectedGames: z.string(),
      createList: z.string(),
      listCreatedToast: z.string(),
      deleteList: z.string(),
      deleteListTitle: z.string(),
      deleteListDescription: z.string(),
      listDeletedToast: z.string(),
      savedEmpty: z.string(),
      savedEmptyDescription: z.string(),
      whitelistEmpty: z.string(),
      whitelistEmptyDescription: z.string(),
      listEmpty: z.string(),
      emptyCta: z.string(),
      loadError: z.string(),
      actionsTitle: z.string(),
      moreInfo: z.string(),
      viewProgress: z.string(),
      updateProgress: z.string(),
      addToList: z.string(),
      removeFromLibrary: z.string(),
      removeFromWhitelist: z.string(),
      addToLibrary: z.string(),
      removeFromList: z.string(),
      noProgress: z.string(),
      progressSummary: z.string(),
      progressStateLabel: z.string(),
      progressDurationLabel: z.string(),
      progressDurationHint: z.string(),
      progressCardLabel: z.string(),
      saveChanges: z.string(),
      listChoiceLabel: z.string(),
      openGame: z.string(),
      listsUpdatedToast: z.string(),
      confirmRemoveTitle: z.string(),
      confirmRemoveDescription: z.string(),
      confirmRemoveListDescription: z.string(),
      confirmRemove: z.string(),
      removedToast: z.string(),
      removedWhitelistToast: z.string(),
      addedToast: z.string(),
      removedFromListToast: z.string(),
      actionFailed: z.string(),
      releaseLabel: z.string(),
      released: z.string(),
    }),
    settings: z.object({
      title: z.string(),
      email: z.object({
        title: z.string(),
        verified: z.string(),
        unverified: z.string(),
        verifyCta: z.string(),
        modalTitle: z.string(),
        sentTo: z.string(),
        confirm: z.string(),
        resend: z.string(),
        resendIn: z.string(),
        successToast: z.string(),
      }),
      consoles: z.object({
        title: z.string(),
        description: z.string(),
        save: z.string(),
        savedToast: z.string(),
      }),
      password: z.object({
        title: z.string(),
        description: z.string(),
        cta: z.string(),
        retryIn: z.string(),
        sentToast: z.string(),
      }),
      notifications: z.object({
        title: z.string(),
        description: z.string(),
        enable: z.string(),
      }),
    }),
    onboarding: z.object({
      stepLabel: z.string(),
      next: z.string(),
      finish: z.string(),
      steps: z
        .array(
          z.object({
            eyebrow: z.string(),
            title: z.string(),
            description: z.string(),
          }),
        )
        .length(4),
    }),
    errorModal: z.object({
      title: z.string(),
      dismiss: z.string(),
    }),
    sessionExpired: z.object({
      title: z.string(),
      description: z.string(),
      action: z.string(),
    }),
    verifyBanner: z.object({
      text: z.string(),
      cta: z.string(),
      dismiss: z.string(),
    }),
    auth: z.object({
      loginTitle: z.string(),
      loginDescription: z.string(),
      registerTitle: z.string(),
      registerDescription: z.string(),
      forgotTitle: z.string(),
      forgotDescription: z.string(),
      emailLabel: z.string(),
      emailPlaceholder: z.string(),
      passwordLabel: z.string(),
      passwordPlaceholder: z.string(),
      nicknameLabel: z.string(),
      nicknamePlaceholder: z.string(),
      submitLogin: z.string(),
      submitRegister: z.string(),
      submitForgot: z.string(),
      forgotLink: z.string(),
      googleContinue: z.string(),
      googleUnavailable: z.string(),
      orSeparator: z.string(),
      socialSeparator: z.string(),
      showPassword: z.string(),
      hidePassword: z.string(),
      errEmail: z.string(),
      errPassword: z.string(),
      errNickname: z.string(),
      noAccountPrompt: z.string(),
      haveAccountPrompt: z.string(),
      forgotSentTitle: z.string(),
      forgotSentDescription: z.string(),
      backToLogin: z.string(),
    }),
  }),
})

export type Dictionary = z.infer<typeof dictionarySchema>
export type ErrorByCodeKey = keyof Dictionary['errors']['byCode']

const loaders: Record<Locale, () => Promise<unknown>> = {
  en: () => import('./dictionaries/en.json').then((m) => m.default),
  es: () => import('./dictionaries/es.json').then((m) => m.default),
}

export async function getDictionary(locale: Locale): Promise<Dictionary> {
  const raw = await (loaders[locale] ?? loaders[defaultLocale])()
  return dictionarySchema.parse(raw)
}
